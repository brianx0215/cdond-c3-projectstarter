- name: "Create directory"
  become: yes
  shell: mkdir -p ~/udapeople/

- name: "deploy backend"
  become: yes
  unarchive:
    src: /etc/udapeople/app.tar.gz
    dest: ~/udapeople/

- name: "run backend"
  become: yes
  shell: |
    cd ~/udapeople/
    npm i    
    pm2 start main.js