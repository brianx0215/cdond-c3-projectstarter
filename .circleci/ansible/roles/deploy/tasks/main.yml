- name: "deploy backend"
  become: yes
  copy:
    src: /etc/udapeople/
    dest: /etc/udapeople/
    remote_src: yes

- name: "run backend"
  become: yes
  shell: |
    cd /etc/udapeople/
    npm i    
    pm2 start main.js